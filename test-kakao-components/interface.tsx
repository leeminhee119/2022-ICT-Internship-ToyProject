export interface ThumbnailInterface {
    image_src?: string,
    title: string,
    info_value?: string,
    is_all_free: boolean,
    is_waitfree: boolean,
    is_waitfree_plus: boolean,
    age_grade: number,
    view_count: number,
    rank?: number,
}